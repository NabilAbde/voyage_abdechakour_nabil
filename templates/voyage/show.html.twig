{% extends 'base.html.twig' %}

{% block title %}Voyage{% endblock %}

{% block body %}
    <h1>Voyage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id du sejour</th>
                <td>{{ voyage.id }}</td>
            </tr>
            <tr>
                <th>Catégorie du séjour</th>
                <td>{{ voyage.categorie.name }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ voyage.name }}</td>
            </tr>
            <tr>
                <th>Accroche</th>
                <td>{{ voyage.accroche }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ voyage.description }}</td>
            </tr>
            <tr>
                <th>Priceperperson</th>
                <td>{{ voyage.priceperperson }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ voyage.duree }} jours </td>
            </tr>
            <tr>
                <th>Image1</th>
                <td><img src="{{ asset(voyage.image1) }}" alt ="coteazur"/></td>
                
            </tr>
            <tr>
                <th>Image2</th>
                <td><img src="{{ asset(voyage.image2) }}" alt ="coteazur"/></td>
            </tr>
            <tr>
                <th>Image3</th>
                <td><img src="{{ asset(voyage.image3) }}" alt ="coteazur"/></td>
            </tr>
            <tr>
                <th>Documentation</th>
                <td><a href="{{ voyage.documentation }}" download> telechargez la brochure </a></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('voyage_index') }}">back to list</a>

    <a href="{{ path('voyage_edit', {'id': voyage.id}) }}">edit</a>

    {{ include('voyage/_delete_form.html.twig') }}
{% endblock %}
